<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_lead_engine" model="ir.ui.view">
        <field name="name">res.config.settings.form.lead.engine</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="crm.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//app[@name='crm']" position="inside">
                <block title="Lead Engine" name="lead_engine_settings">
                    <div class="alert alert-warning mb-3" role="alert" colspan="2">
                        Very low limits make troubleshooting rejected payloads harder; very high limits increase database
                        storage for intake logs.
                    </div>
                    <setting
                        id="lead_engine_payload_setting"
                        string="Intake payload max length"
                        help="Maximum characters stored on each intake log raw payload (truncation)."
                    >
                        <field name="lead_engine_payload_max_chars"/>
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
