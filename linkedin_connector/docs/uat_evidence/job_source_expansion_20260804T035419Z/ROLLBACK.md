1. SQL/UI: UPDATE linkedin_job_source_connector SET enabled=false;
2. Keep ir_cron_job_source_scheduler inactive
3. Restore dump if schema rollback required
4. Module version prior to 19.0.2.20.0 if needed
