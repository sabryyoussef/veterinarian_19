<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_vst_invoice_backfill_wizard" model="ir.ui.view">
        <field name="name">petspot.vendor.sell.through.invoice.backfill.wizard.form</field>
        <field name="model">petspot.vendor.sell.through.invoice.backfill.wizard</field>
        <field name="arch" type="xml">
            <form string="Invoice Commercial Backfill">
                <group>
                    <field name="company_id"/>
                    <field name="cutoff_date"/>
                    <field name="dry_run"/>
                    <field name="vendor_bill_id"/>
                </group>
                <group string="Result" invisible="not result_summary">
                    <field name="result_summary" nolabel="1" colspan="2"/>
                    <field name="result_json" nolabel="1" colspan="2" widget="text"/>
                </group>
                <footer>
                    <button name="action_run" type="object" string="Run" class="btn-primary"/>
                    <button string="Close" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_vst_invoice_backfill_wizard" model="ir.actions.act_window">
        <field name="name">Invoice Commercial Backfill</field>
        <field name="res_model">petspot.vendor.sell.through.invoice.backfill.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
